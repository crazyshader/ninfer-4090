# NInfer-4090 方案B 启动脚本说明

编译产物 + 一键启动脚本都在本目录 `E:\ai\ninfer-4090-native\`。

## 启动脚本（双击即用）

| 脚本 | 档位 | 用途 |
|------|------|------|
| `run-e8-128k.bat` | E8 @ 128K · MTP3 | **日常推荐**，综合最优 |
| `run-int8-128k.bat` | INT8 @ 128K · MTP3 | 要极致数值精度时用 |
| `run-e8-longctx.bat` | E8 @ 320K · MTP4 | 长文档 / RAG / 大代码库 |
| `run-vision.bat` | Vision @ 280K · MTP4 | 需要图像/视频输入时才用 |
| `smoketest.bat` | — | 服务起来后，验证 API 是否正常 |

## 用法

1. 双击任一 `run-*.bat`，等日志出现 `listening on http://127.0.0.1:8080` 即就绪
   （冷加载约 6 秒）。
2. API 地址：`http://127.0.0.1:8080`，兼容 OpenAI（`/v1/chat/completions`、`/v1/responses`）
   和 Anthropic（`/v1/messages`）。
3. 另开一个窗口双击 `smoketest.bat` 验证，或直接让你的客户端指向 8080 端口。
4. 停止服务：在服务窗口按 `Ctrl+C`。

## 注意事项

- **一次只跑一个档**：都独占 24GB 显存单请求，别同时开两个 `run-*.bat`。
- **和方案 A（Docker）互斥**：跑方案 B 前先停 Docker 的 ninfer 服务，否则抢显存 OOM。
- **改档位/参数**：直接编辑对应 `.bat` 里的 `--max-context`、`--kv-dtype`、`--draft-tokens`。
  KV 可选：`int8` / `rk8v4` / `rk4v4` / `rk4v4-e8`（4-bit E8）/ `rk2v4-e8`（2-bit E8，冲最长上下文）。
- **权重路径**：脚本里 `MODEL` 指向 `E:\ai\ninfer-4090\qwen3_8_27b_8-19.ninfer`，换权重改这一行。
- **CLI 单条推理**（不起服务）：用 `build-ninja\apps\ninfer.exe`，中文 prompt 必须用
  `--messages xxx.json`（UTF-8 文件），不能用 `--prompt` 直传中文（会编码报错）。

详见部署文档 `.kiro/docs/程序/ninfer-4090部署实施方案.md` 的「## 11 方案 B」章节。
